---
phase: 05-integracao-real-do-grafico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart
  - direcao_financeira_mobile/lib/app/domain/repositories/i_transaction_repository.dart
  - direcao_financeira_mobile/lib/app/data/datasources/transaction_datasource.dart
  - direcao_financeira_mobile/lib/app/data/repositories/transaction_repository.dart
  - direcao_financeira_mobile/lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart
  - direcao_financeira_mobile/lib/app/data/providers/nest/finance/nest_transaction_remote_datasource.dart
  - direcao_financeira_mobile/lib/app/presentation/modules/home/home_controller.dart
  - direcao_financeira_mobile/lib/app/presentation/modules/home/home_expense_chart_item.dart
  - direcao_financeira_mobile/lib/app/presentation/modules/home/widgets/expenses_chart_section.dart
  - direcao_financeira_mobile/test/app/data/providers/supabase/finance/supabase_transaction_remote_datasource_test.dart
  - direcao_financeira_mobile/test/app/presentation/modules/home/home_controller_test.dart
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
requirements_addressed:
  - HOME-01
  - HOME-02
  - HOME-03
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Ao abrir a home com provider Supabase, o grafico busca no Supabase apenas as transacoes do mes selecionado e deixa de depender de um dataset residual em memoria."
    - "Ao trocar o mes na home, o controller dispara nova carga remota para o periodo selecionado e o grafico passa a refletir exatamente esse retorno mensal."
    - "O bloco do grafico nao nasce mais de placeholder fixo nem de `List<Map<String, dynamic>>` hardcoded no `HomeController`."
  artifacts:
    - path: "direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart"
      provides: "Contrato de caso de uso capaz de pedir transacoes por mes selecionado"
      min_lines: 20
    - path: "direcao_financeira_mobile/lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart"
      provides: "Consulta Supabase com filtro de periodo mensal na origem"
      min_lines: 80
    - path: "direcao_financeira_mobile/lib/app/presentation/modules/home/home_controller.dart"
      provides: "Orquestracao da home que recarrega o mes selecionado e agrega o grafico sobre o retorno mensal"
      min_lines: 150
    - path: "direcao_financeira_mobile/lib/app/presentation/modules/home/home_expense_chart_item.dart"
      provides: "Contrato tipado das fatias do grafico"
      min_lines: 20
  key_links:
    - from: "direcao_financeira_mobile/lib/app/presentation/modules/home/home_controller.dart"
      to: "direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart"
      via: "loadDashboardData(selectedMonth) chama GetTransactionsUseCase com o mes corrente"
      pattern: "getTransactionsUseCase\\("
    - from: "direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart"
      to: "direcao_financeira_mobile/lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart"
      via: "Contrato propagado ate o datasource real do Supabase"
      pattern: "getTransactions"
    - from: "direcao_financeira_mobile/lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart"
      to: "SupabaseTableNames.transactions"
      via: "Filtro por limites do mes aplicado diretamente na query"
      pattern: "transactionDate"
    - from: "direcao_financeira_mobile/lib/app/presentation/modules/home/widgets/expenses_chart_section.dart"
      to: "direcao_financeira_mobile/lib/app/presentation/modules/home/home_expense_chart_item.dart"
      via: "Renderizacao baseada em contrato tipado, sem maps dinamicos"
      pattern: "HomeExpenseChartItem"
---

<objective>
Ativar o grafico de gastos da home com dados mensais reais vindos da fonte remota correta, garantindo que `HOME-01` e `HOME-02` fechem com uma busca por periodo no contrato atual em vez de uma simples recomputacao sobre `ultimasTransacoes`.

Purpose: eliminar a ambiguidade do plano atual e amarrar explicitamente a fase a uma garantia verificavel de origem mensal real no Supabase.
Output: contrato de transacoes aceitando periodo mensal, datasource Supabase filtrando na query, `HomeController` recarregando o mes selecionado e widget do grafico consumindo estado tipado sem mock.
</objective>

<execution_context>
@C:/Users/Samuel Vitor/Documents/aplicativos/direcao_financeira/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Samuel Vitor/Documents/aplicativos/direcao_financeira/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart
@direcao_financeira_mobile/lib/app/domain/repositories/i_transaction_repository.dart
@direcao_financeira_mobile/lib/app/data/datasources/transaction_datasource.dart
@direcao_financeira_mobile/lib/app/data/repositories/transaction_repository.dart
@direcao_financeira_mobile/lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart
@direcao_financeira_mobile/lib/app/data/providers/nest/finance/nest_transaction_remote_datasource.dart
@direcao_financeira_mobile/lib/app/presentation/modules/home/home_controller.dart
@direcao_financeira_mobile/lib/app/presentation/modules/home/home_binding.dart
@direcao_financeira_mobile/lib/app/presentation/modules/home/home_view.dart
@direcao_financeira_mobile/lib/app/presentation/modules/home/widgets/month_selector.dart
@direcao_financeira_mobile/lib/app/presentation/modules/home/widgets/expenses_chart_section.dart

<interfaces>
From `transaction_use_cases.dart`:
```dart
class GetTransactionsUseCase {
  Future<Either<Failure, List<TransactionEntity>>> call() async
}
```

From `i_transaction_repository.dart`:
```dart
abstract class ITransactionRepository {
  Future<Either<Failure, List<TransactionEntity>>> getTransactions();
}
```

From `transaction_datasource.dart`:
```dart
abstract class ITransactionDataSource {
  Future<List<TransactionModel>> getTransactions();
}
```

From `home_controller.dart`:
```dart
final selectedMonth = DateTime.now().obs;
final ultimasTransacoes = <TransactionEntity>[].obs;
Future<void> loadDashboardData({bool silent = false}) async
void previousMonth()
void nextMonth()
```

From `transaction_entity.dart`:
```dart
enum TransactionType { income, expense }

class TransactionEntity {
  final int id;
  final int categoryId;
  final int amountCents;
  final DateTime transactionDate;
  final String? categoryName;
  final String? categoryColor;
  double get amount => amountCents / 100.0;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajustar o contrato de transacoes para busca mensal real do periodo selecionado</name>
  <read_first>
    - direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart
    - direcao_financeira_mobile/lib/app/domain/repositories/i_transaction_repository.dart
    - direcao_financeira_mobile/lib/app/data/datasources/transaction_datasource.dart
    - direcao_financeira_mobile/lib/app/data/repositories/transaction_repository.dart
    - direcao_financeira_mobile/lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart
    - direcao_financeira_mobile/lib/app/data/providers/nest/finance/nest_transaction_remote_datasource.dart
    - direcao_financeira_mobile/test/app/data/providers/supabase/finance/supabase_transaction_remote_datasource_test.dart
  </read_first>
  <files>
    direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart
    direcao_financeira_mobile/lib/app/domain/repositories/i_transaction_repository.dart
    direcao_financeira_mobile/lib/app/data/datasources/transaction_datasource.dart
    direcao_financeira_mobile/lib/app/data/repositories/transaction_repository.dart
    direcao_financeira_mobile/lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart
    direcao_financeira_mobile/lib/app/data/providers/nest/finance/nest_transaction_remote_datasource.dart
    direcao_financeira_mobile/test/app/data/providers/supabase/finance/supabase_transaction_remote_datasource_test.dart
  </files>
  <behavior>
    - Teste 1: ao pedir marco de 2026, o datasource Supabase monta query limitada ao inicio e ao fim desse mes, sem buscar todos os registros do usuario.
    - Teste 2: o contrato `GetTransactionsUseCase -> ITransactionRepository -> ITransactionDataSource` propaga o mes selecionado sem descartar o filtro.
    - Teste 3: o provider Nest continua compilando com o novo contrato e honra o periodo por query params ou, se a API nao suportar filtro, por fallback local documentado no proprio datasource sem quebrar a assinatura.
  </behavior>
  <action>
    Introduzir no contrato de transacoes um parametro explicito de periodo mensal, preferencialmente um `DateTime referenceMonth` ou um value object equivalente simples, mantendo o nome coerente em `GetTransactionsUseCase`, `ITransactionRepository`, `TransactionRepository` e `ITransactionDataSource`.

    Em `supabase_transaction_remote_datasource.dart`, implementar a busca mensal real na origem: calcular `startOfMonth` e `startOfNextMonth` a partir do mes pedido e aplicar o filtro diretamente na query de `transactions`, por exemplo com limites equivalentes a `gte(transactionDate, startOfMonth)` e `lt(transactionDate, startOfNextMonth)`. O plano precisa deixar explicito que esse passo e o fechamento de `HOME-01` e `HOME-02`: o grafico da home passa a depender do dataset mensal retornado pelo Supabase, nao de uma lista historica carregada sem escopo temporal.

    Em `nest_transaction_remote_datasource.dart`, adaptar a mesma assinatura. Se o endpoint Nest ja aceitar periodo, enviar os parametros de inicio/fim na request. Se nao aceitar, manter compatibilidade com fallback local no datasource e registrar isso no resumo da execucao, sem relaxar a implementacao do provider Supabase, que segue obrigatoriamente filtrando na origem nesta fase.

    Criar o teste automatizado do datasource Supabase validando os limites do mes e o nao uso de uma consulta aberta sem periodo. Nao deixar a garantia apenas em `flutter analyze`.
  </action>
  <verify>
    <automated>flutter test test/app/data/providers/supabase/finance/supabase_transaction_remote_datasource_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `GetTransactionsUseCase` nao possui mais assinatura sem periodo para a carga usada pela home.
    - `SupabaseTransactionRemoteDataSource.getTransactions(...)` aplica filtro mensal diretamente na consulta de `transactions`.
    - O teste automatizado prova que o mes pedido gera limites corretos de consulta.
    - O comando automatizado termina com codigo 0.
  </acceptance_criteria>
  <done>Existe uma garantia concreta e verificavel de que o dataset usado pelo grafico vem do Supabase ja filtrado para o mes selecionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fazer a home recarregar o mes selecionado e agregar o grafico apenas sobre o retorno mensal</name>
  <read_first>
    - direcao_financeira_mobile/lib/app/presentation/modules/home/home_controller.dart
    - direcao_financeira_mobile/lib/app/presentation/modules/home/home_view.dart
    - direcao_financeira_mobile/lib/app/presentation/modules/home/widgets/expenses_chart_section.dart
    - direcao_financeira_mobile/lib/app/domain/usecases/transaction_use_cases.dart
    - direcao_financeira_mobile/test/app/presentation/modules/home/home_controller_test.dart
  </read_first>
  <files>
    direcao_financeira_mobile/lib/app/presentation/modules/home/home_controller.dart
    direcao_financeira_mobile/lib/app/presentation/modules/home/home_expense_chart_item.dart
    direcao_financeira_mobile/test/app/presentation/modules/home/home_controller_test.dart
  </files>
  <behavior>
    - Teste 1: ao iniciar a home, `loadDashboardData()` chama `getTransactionsUseCase` com `selectedMonth.value`.
    - Teste 2: `previousMonth()` e `nextMonth()` atualizam `selectedMonth` e disparam nova carga remota para o novo mes, em vez de apenas recalcular sobre dados antigos.
    - Teste 3: o grafico e reconstruido apenas com transacoes de despesa retornadas para o periodo e nao nasce mais de placeholder fixo.
  </behavior>
  <action>
    Criar `home_expense_chart_item.dart` com um contrato tipado e imutavel para cada fatia do grafico, contendo exatamente `categoryId`, `categoryLabel`, `amountCents`, `percentage` e `color`. Em `home_controller.dart`, remover o array hardcoded de `gastosPorCategoria` e substituir por `RxList<HomeExpenseChartItem>`.

    Atualizar `loadDashboardData({bool silent = false})` para sempre pedir transacoes com `getTransactionsUseCase(selectedMonth.value)` no fluxo principal da home. O controller deve continuar carregando contas e cartoes como hoje, mas o bloco de transacoes/grafico passa a tratar `ultimasTransacoes` como o dataset do mes atual retornado pela camada de dados. `previousMonth()` e `nextMonth()` devem alterar `selectedMonth` e, em seguida, chamar `loadDashboardData(silent: true)` para buscar novamente o periodo; nao aceitar apenas recomputacao local sobre um cache historico indefinido.

    Implementar um metodo privado para transformar o retorno mensal em fatias do grafico: filtrar `TransactionType.expense`, agrupar por `categoryId`, somar `amountCents`, ordenar por valor decrescente, calcular `percentage`, usar `categoryName` quando existir e fallback `Categoria #<id>` quando vier nulo, e resolver cor por `categoryColor` ou paleta deterministica local. `totalSaidas` deve passar a derivar do novo contrato tipado.

    Criar teste do controller cobrindo a chamada do caso de uso com o mes selecionado na carga inicial e na navegacao entre meses. Esse teste e a prova local de `HOME-02` na camada de presentation.
  </action>
  <verify>
    <automated>flutter test test/app/presentation/modules/home/home_controller_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `HomeController` chama `getTransactionsUseCase(selectedMonth.value)` na carga inicial e ao trocar o mes.
    - `previousMonth()` e `nextMonth()` nao apenas recalculam sobre `ultimasTransacoes` antigas; eles disparam nova busca do periodo.
    - `gastosPorCategoria` nao contem mais placeholder fixo e usa lista tipada.
    - O comando automatizado termina com codigo 0.
  </acceptance_criteria>
  <done>A home passa a buscar e refletir o dataset mensal real do periodo selecionado, fechando `HOME-01` e `HOME-02` na orquestracao do controller.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar o widget do grafico para o contrato tipado sem maps dinamicos</name>
  <read_first>
    - direcao_financeira_mobile/lib/app/presentation/modules/home/widgets/expenses_chart_section.dart
    - direcao_financeira_mobile/lib/app/presentation/modules/home/home_expense_chart_item.dart
    - direcao_financeira_mobile/lib/app/presentation/modules/home/home_controller.dart
    - direcao_financeira_mobile/test/app/presentation/modules/home/home_controller_test.dart
  </read_first>
  <files>
    direcao_financeira_mobile/lib/app/presentation/modules/home/widgets/expenses_chart_section.dart
    direcao_financeira_mobile/lib/app/presentation/modules/home/home_expense_chart_item.dart
  </files>
  <action>
    Atualizar `expenses_chart_section.dart` para consumir `List<HomeExpenseChartItem>` e eliminar todo acesso por chave string como `g['categoria']`, `g['valor']`, `g['percentual']` e `g['cor']`. O widget deve ler `categoryLabel`, `percentage`, `color` e o total vindo do controller.

    Ajustar `_buildChart`, `_buildLegend` e `_DonutChartPainter` para operar apenas com o contrato tipado. No texto central do donut, continuar exibindo o percentual da maior categoria do mes quando a lista tiver itens; quando a lista estiver vazia, renderizar `0%` sem reintroduzir placeholder fixo. Manter o layout atual do card, sem tentar resolver loading, vazio ou erro nesta fase, porque esses estados pertencem explicitamente a fase 6.
  </action>
  <verify>
    <automated>flutter analyze lib/app/presentation/modules/home/widgets/expenses_chart_section.dart lib/app/presentation/modules/home/home_expense_chart_item.dart lib/app/presentation/modules/home/home_controller.dart lib/app/domain/usecases/transaction_use_cases.dart lib/app/domain/repositories/i_transaction_repository.dart lib/app/data/datasources/transaction_datasource.dart lib/app/data/repositories/transaction_repository.dart lib/app/data/providers/supabase/finance/supabase_transaction_remote_datasource.dart lib/app/data/providers/nest/finance/nest_transaction_remote_datasource.dart</automated>
  </verify>
  <acceptance_criteria>
    - `ExpensesChartSection` nao usa mais `Map<String, dynamic>` para renderizar o grafico.
    - O percentual central e a legenda saem da lista tipada derivada do retorno mensal buscado pelo controller.
    - O layout visual atual do card e preservado.
    - O comando automatizado termina com codigo 0.
  </acceptance_criteria>
  <done>O widget do grafico renderiza o estado tipado produzido pela integracao mensal real, sem dependencia de estrutura dinamica ou mock fixo.</done>
</task>

</tasks>

<verification>
- [ ] `HOME-01`, `HOME-02` e `HOME-03` ficam cobertos no proprio plano.
- [ ] O plano exige filtro mensal real no datasource Supabase, nao apenas recomputacao sobre `ultimasTransacoes`.
- [ ] A troca de mes dispara nova busca remota do periodo selecionado.
- [ ] Existe verificacao automatizada especifica para o contrato mensal e para o controller da home.
</verification>

<success_criteria>
- Ao concluir o plano, `HomeController` nao contem mais o item fixo de `Manutencao`.
- O grafico da home usa dados mensais reais vindos de uma busca filtrada por periodo no Supabase.
- O bloco respeita o mes selecionado porque a home recarrega o periodo na origem e nao porque reutiliza um dataset historico indefinido.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integracao-real-do-grafico/05-01-SUMMARY.md`
</output>
